# Gemini Developer API key — get one free at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the model (default below). Search grounding requires a
# Gemini model that supports the googleSearch tool.
GEMINI_MODEL=gemini-3.6-flash
